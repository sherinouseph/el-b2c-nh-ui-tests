package com.englishtown.pages.core;

import org.openqa.selenium.WebDriver;

public class LandingPage extends EnglishtownPage {
    protected LandingPage(WebDriver webDriver, String url) {
        super(webDriver, url);
    }
}
